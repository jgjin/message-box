[project]
name = "message-box"
version = "0.1.0"
description = "a box for messages"
requires-python = ">=3.11"
dependencies = [
    "esptool>=5.1.0",
    "fastapi[standard]>=0.128.0",
    "mip>=1.15.0",
    "mpremote>=1.27.0",
    "uvicorn>=0.40.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]
